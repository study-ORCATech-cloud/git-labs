name: Use Repository Secrets

# TODO: Configure this workflow to trigger on push events
on:
  # Add your trigger configuration here

jobs:
  access-secrets:
    runs-on: ubuntu-latest
    steps:
      # TODO: Add a step that accesses a repository secret as an environment variable
      # Hint: Use the secrets context and env to pass the secret
      
      # TODO: Add a step that demonstrates using a secret with an external API (simulated)
      # Hint: Remember not to print the actual secret value in logs
      
      # TODO: Add a step that shows how to pass a secret to an action
      # Hint: Many actions accept secrets via their 'with' parameters or environment variables 